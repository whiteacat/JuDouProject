<view class="container">
  <view class="title">创建群组</view>
  <input
    class="input"
    placeholder="群组名称，如：产品部"
    value="{{name}}"
    bindinput="onNameInput"
    maxlength="64"
  />
  <button class="submit" bindtap="onSubmit">创建</button>
</view>
